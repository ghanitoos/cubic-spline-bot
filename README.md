# 🦆 Cubic Spline Educational Bot

این پروژه یک ربات تلگرام آموزشی است که مفاهیم **درونیابی اسپلاین مکعبی (Cubic Spline Interpolation)** را به دانشجویان و علاقه‌مندان به محاسبات عددی آموزش می‌دهد.

## ✨ ویژگی‌ها

- **آموزش تئوری:** توضیحات کامل ریاضی درباره روش اسپلاین، شرط‌های مرزی و ماتریس‌های سه قطری.
- **تحلیل کد:** نمایش و تحلیل خط به خط کد پایتون برای پیاده‌سازی الگوریتم.
- **حل مسئله (دو حالت):**
    - **ساده (=f(x)$):** برای توابع ریاضی معمولی.
    - **پارامتری ((t), y(t)$):** برای رسم اشکال پیچیده (مانند پروفایل ایرفویل یا اردک).
- **نمودار:** رسم نمودار دقیق نقاط و منحنی برازش شده.

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها
- Python 3.8+
- یک توکن ربات تلگرام (از @BotFather بگیرید)

### مراحل نصب

1. مخزن را کلون کنید:
   ```bash
   git clone https://github.com/your-username/cubic-spline-bot.git
   cd cubic-spline-bot
   ```

2. محیط مجازی بسازید و فعال کنید:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

3. وابستگی‌ها را نصب کنید:
   ```bash
   pip install -r requirements.txt
   ```

4. فایل تنظیمات را بسازید:
   - فایل `.env.example` را به `.env` تغییر نام دهید.
   - توکن ربات خود را در آن قرار دهید:
     ```env
     BOT_TOKEN=123456789:ABCDefghIJKLmnOPqrstUVwxyz
     ```

5. ربات را اجرا کنید:
   ```bash
   python bot.py
   ```

## 📚 ساختار پروژه

- `bot.py`: فایل اصلی و هندلرهای تلگرام.
- `spline_logic.py`: کلاس‌های محاسباتی ( و ).
- `content.py`: متون آموزشی و پیام‌های فارسی.

## 🤝 مشارکت

اگر ایده‌ای برای بهبود دارید، خوشحال می‌شویم که Pull Request بفرستید!

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.
